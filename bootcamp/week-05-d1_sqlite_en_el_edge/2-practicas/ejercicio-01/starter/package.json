{
  "name": "ejercicio-01-d1-raw-sql",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:migrate:local": "wrangler d1 migrations apply movies-db --local",
    "db:seed:local": "wrangler d1 execute movies-db --local --file=./seed.sql"
  },
  "dependencies": {
    "hono": "4.12.18",
    "zod": "4.4.3",
    "@hono/zod-validator": "0.8.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20260511.1",
    "typescript": "6.0.3",
    "wrangler": "4.90.1"
  }
}
